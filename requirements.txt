# NEURAL EVASION — Requirements 
# Python 3.10 – 3.12 recommended
#
# INSTALLATION ORDER MATTERS for PyTorch Geometric.
# Follow the steps below instead of a plain `pip install -r requirements.txt`.
#
# Step 1 — PyTorch (CPU-only, sufficient to play the game):
#   pip install torch>=2.1.0 --index-url https://download.pytorch.org/whl/cpu
#
#   For CUDA training on Colab / local GPU, let Colab handle torch or use:
#   pip install torch>=2.1.0 --index-url https://download.pytorch.org/whl/cu121
#
# Step 2 — PyTorch Geometric (must know your torch version first):
#   pip install torch_geometric
#   pip install pyg_lib torch_scatter torch_sparse \
#       -f https://data.pyg.org/whl/torch-$(python -c "import torch; print(torch.__version__)").html \
#       --only-binary :all: \
#       || echo "Optional C++ extensions skipped — pure Python fallback used."
#
# Step 3 — Everything else:
#   pip install -r requirements.txt
#
# #########################################################################

# Game runtime
pygame>=2.1.0

# Deep learning core
torch>=2.1.0
torch_geometric>=2.4.0

# Numerical
numpy>=1.24.0

# Training utilities (Colab / local training only — not needed just to play)
matplotlib>=3.7.0
tqdm>=4.65.0
scikit-learn>=1.3.0
